# قواعد التطوير - نظام إدارة مصنع المطابخ

## القواعد الأساسية

### 1. قاعدة الملف الواحد للمسارات (Single Source of Truth)
- **المشكلة**: كان يوجد `app.py` و `app_part1.py` إلى `app_part4.py` مع تعريفات متضاربة
- **القاعدة**: 
  - الملف الرئيسي للتطبيق (مثل `app.py`، `main.py`، `index.ts`) هو المرجع الوحيد الذي يجمع تسجيل المسارات/النهايات (Endpoints) وتعريفات النماذج/المخططات
  - لا توجد ملفات مسارات منفصلة أو مكررة خارج إطار الوحدات الرسمية التي يُعاد استيرادها وتسجيلها عبر الملف الرئيسي
  - جميع الـ API endpoints تُعرَّف خارج أي كتل تنفيذ خاصة (مثل `if __name__ == '__main__':` في بايثون أو `server.listen` في Node) لضمان إمكانية إعادة الاستخدام/الاختبار
  - يُسمح بتنظيم المنطق داخل وحدات أو حزم فرعية (Blueprints، Routers، Controllers)، مع تسجيلها بوضوح في الملف الرئيسي للحفاظ على مصدر واحد للحقيقة
  - يُقسَّم الملف الرئيسي إلى أقسام واضحة (تهيئة الإطار/الخدمات، تعريف النماذج، تسجيل المسارات، خدمات مساعدة) مع تعليقات أو عناوين داخلية توضّح هيكلة الملف
  - تتوفر وثيقة إرشادية مختصرة تشرح كيفية إضافة مسار جديد أو وحدة فرعية بحيث يلتزم جميع المطورين بنفس التدفق أيّاً كان إطار العمل المستخدم

### 2. قاعدة توحيد قاعدة البيانات (Database Consistency)
- **المشكلة**: تضارب في مسارات قاعدة البيانات بين `kitchen_factory.db` و `instance/kitchen_factory.db`
- **القاعدة**:
  - مسار أو إعداد اتصال واحد ثابت لكل بيئة (تطوير، اختبار، إنتاج) يُعرّف في مصدر إعدادات مركزي (ملفات إعداد، `.env`، متغيرات بيئة) مع توثيق طريقة تحميله
  - استخدام نظام ترحيلات معتمد (Alembic، Django Migrations، Prisma Migrate، Liquibase...) بدلاً من تنفيذ SQL يدوي لضمان أثر زمني واضح
  - تجنّب أسماء الجداول أو الحقول المتضاربة مع الكلمات المحجوزة في محرك قاعدة البيانات المستهدف، مع توثيق الاستثناءات
  - جميع السكربتات والخدمات تستخدم نفس مُعرّف الاتصال (Connection String) أو إعداد ORM المُعرّف في المصدر المركزي، ولا يُعاد تعريفه محلياً
  - تشغيل اختبارات أو سكربتات تحقق لتكامل قاعدة البيانات قبل الدمج لضمان توافق الترجمات مع البنية الفعلية
  - توفير دليل عملي يشرح دورة حياة الترحيلات (إنشاء، مراجعة، تطبيق، تراجع) لأي إطار أو أداة مستخدمة، مع ذكر بدائل مكافئة حسب التقنية

### 3. قاعدة التحقق من الصلاحيات والبيانات (Input Validation & Security)
- **المشكلة**: عدم وجود CSRF protection، أذونات غير متسقة، تحقق ضعيف من المدخلات
- **القاعدة**:
  - جميع الطلبات المعدِّلة للحالة (POST/PUT/PATCH/DELETE) يجب أن تمر عبر حماية CSRF أو آليات حماية مكافئة (مثل حزم التوقيع أو توكنات Anti-forgery) في جميع الأطر (Flask، Django، FastAPI، إلخ)
  - التحقق من الصلاحيات في كل نقطة دخول حساسة، مع اتباع مبدأ أقل الصلاحيات وإعادة التحقق من الجلسة عند العمليات الحرجة
  - التحقق من صحة البيانات المدخلة باستخدام طبقة Validators موحدة (مثل Pydantic، Marshmallow، Django Forms، أو مكتبات مدمجة في الإطار)، مع تحديد مخططات واضحة للأنواع والتواريخ والملفات
  - تقييد أنواع الملفات المرفوعة على مستوى الخادم والتحقق من امتدادها ومحتواها الفعلي، بالإضافة إلى تحديد حجم أقصى ومجلدات آمنة
  - تسجيل المحاولات الفاشلة للوصول أو التحقق، مع ربطها بنظام تنبيهات أو مراقبة، والتأكد من توفير مسارات تدقيق واضحة
  - حماية الـ API عبر آليات مصادقة معيارية (JWT، OAuth2، مفاتيح API) وإضافة حدود طلب (Rate limiting) وسياسات Timeout للجلسات لتقليل فرص الاستغلال

## قواعد التتبع والتوثيق

### 1. تسجيل التغييرات في Change log.md
**الهدف**: تتبع جميع التعديلات بطريقة سهلة ومفصلة للمستخدم

**المتطلبات**:
- **تاريخ التغيير**: يجب تسجيل التاريخ بالصيغة `YYYY-MM-DD`
- **الملفات المعدلة**: قائمة بجميع الملفات التي تم تعديلها
- **أرقام السطور**: تحديد السطور المضافة أو المعدلة
- **وصف التغيير**: شرح واضح للهدف من التغيير
- **التأثير**: وصف تأثير التغيير على التطبيق

**الاستثناءات** (لا تُسجل في Change log):
- ❌ **ملفات Markdown (.md)**: جميع ملفات التوثيق والخطط لا تحتاج تسجيل في Change log
- ❌ **ملفات README**: تحديثات التوثيق العامة
- ❌ **ملفات .gitignore**: تغييرات إعدادات Git

**صيغة التتبع**:
```markdown
## [التاريخ] - وصف مختصر للتغيير
**الملفات**: 
- `ملف1.py` (السطور X-Y): وصف التغيير
- `ملف2.html` (السطر Z): وصف التغيير

**الهدف**: شرح الهدف من التغيير
**التأثير**: وصف تأثير التغيير على التطبيق
```

### 2. التأكد من التوافق وعدم التكرار
**الهدف**: ضمان استقرار التطبيق وتجنب التعارضات

**المتطلبات**:
- **فحص التوافق**: قبل أي تعديل، تأكد من توافق التغيير مع باقي أجزاء التطبيق
- **منع التكرار**: التحقق من عدم وجود تعريفات مكررة للمسارات أو النماذج
- **اختبار الوظائف**: التأكد من عمل جميع الوظائف المرتبطة بالتغيير
- **فحص القوالب**: التأكد من توافق القوالب مع النماذج والمسارات المعدلة

**خطوات التحقق**:
1. فحص جميع الملفات بحثاً عن التكرار
2. اختبار المسارات المتأثرة
3. التحقق من صحة القوالب
4. التأكد من عمل قاعدة البيانات
5. اختبار الصلاحيات والأذونات

## قواعد إضافية

### 4. قاعدة أمان الملفات
- التحقق من صحة نوع الملف قبل القبول باستخدام كلٍّ من الامتداد وقراءة التوقيع/النوع الحقيقي (MIME sniffing أو Magic numbers) بغض النظر عن الإطار أو نظام التخزين
- تنظيف أسماء الملفات أو استبدالها بمعرّفات آمنة لتفادي هجمات المسارات أو التعارضات، مع حفظ الاسم الأصلي في السجلات عند الحاجة
- تحديد حدود لحجم الملفات وسرعة الرفع (Rate limiting) لتفادي الاستهلاك المفرط للموارد
- اختيار استراتيجية تخزين آمنة تناسب التطبيق (مجلد غير عام، خدمة تخزين سحابي، نظام إدارة ملفات داخلي) مع ضبط صلاحيات الوصول وتشفير الملفات الحساسة عند الضرورة
- تعريف سياسة احتفاظ وحذف تلقائي للملفات غير المستخدمة، مع آلية تدقيق (Audit log) تُسجّل عمليات الرفع والحذف وهوية المستخدم والمسار الزمني

### 5. قاعدة معالجة الأخطاء
- تغليف العمليات الحساسة (قواعد البيانات، الملفات، الشبكات، استدعاءات الخدمات الخارجية) داخل كتل معالجة استثناءات مناسبة (`try/except` أو ما يعادلها في التقنية المستخدمة)، مع تحديد متى يتم التراجع أو إعادة المحاولة
- اعتماد سياسة تسجيل موحدة بمستويات واضحة (DEBUG، INFO، WARNING، ERROR، CRITICAL) وتوجيه السجلات إلى نظام مراقبة مركزي (Sentry، ELK، CloudWatch، Datadog...) لمتابعة الأعطال
- توفير رسائل خطأ ودية للمستخدم لا تكشف تفاصيل حساسة، مع الاحتفاظ بالمعلومات التقنية الكاملة في السجلات الداخلية لأغراض الدعم
- في العمليات التي تغيّر الحالة (معاملات قواعد البيانات، نشر رسائل، تحديثات الملفات)، يجب تنفيذ Rollback أو تعويض مناسب عند وقوع أخطاء لضمان سلامة البيانات
- إضافة اختبارات تغطي سيناريوهات الفشل الحرجة (مثل فشل الحفظ، انقطاع الاتصال، ردود الخدمات الخارجية غير المتوقعة) للتحقق من أن مسارات معالجة الأخطاء تعمل حسب التوقعات

### 6. قاعدة توحيد التواريخ
- تخزين جميع القيم الزمنية بتوقيت UTC على مستوى قواعد البيانات، الرسائل، وسجلات التدقيق لضمان اتساق العمليات الحسابية
- تحويل القيم الزمنية إلى المنطقة المحلية المناسبة عند العرض باستخدام طبقة Formatting متخصصة (مثل date-fns، Moment/Luxon، Java Time، أو مكتبات النظام) مع احترام إعدادات اللغة
- اعتماد تنسيق قياسي موفَّق (مثل ISO 8601 للتبادلات الآلية) وتوفير تنسيقات محلية موثقة للواجهات الأمامية حسب متطلبات السوق
- إدارة المناطق الزمنية في العمليات المجدولة أو المهام الخلفية باستخدام مكتبات واعية بالمنطقة الزمنية وتوثيق الإجراءات لتفادي أخطاء التوقيت الصيفي أو التحويلات
- إضافة اختبارات للحالات الحدّية (التحويل بين المناطق، التوقيت الصيفي، الفواصل الزمنية الطويلة) ضمن خطط الاختبار لضمان دقة المعالجات الزمنية في مختلف التقنيات

### 7. قاعدة الإعدادات الإقليمية (Localization)
- توحيد إعدادات اللغة، العملة، التنسيقات، والمنطقة الزمنية عبر مصدر إعدادات مركزي يمكن تخصيصه لكل سوق أو مستخدم (ملفات إعداد، قاعدة بيانات إعدادات، خدمات Configuration)
- تخزين القيم الرقمية والزمنية بالأشكال المحايدة (أرقام، UTC) وتحويلها عند العرض باستخدام طبقة Localization تعتمد مكتبات مناسبة (مثل ICU، Babel، FormatJS، Intl API)
- إدارة الترجمات عبر ملفات أو أنظمة ترجمة موثّقة (PO/MO، JSON، XLIFF...) مع عملية مراجعة وتحديث دورية، وضمان اكتمال جميع النصوص قبل الإصدار
- دعم اتجاه النص المناسب (RTL أو LTR) واختبار الواجهات للتأكد من توافق التصميم مع كلا الاتجاهين
- معالجة الفروق المحلية في التواريخ، الأرقام، الهواتف، العناوين، والعملات عبر وحدات Formatting قابلة للتوسعة، مع توفير أمثلة مبدئية لأي سوق مستهدف
- تضمين اختبارات توطين (واجهة، محتوى، قابلية إدخال البيانات) للتحقق من أن التطبيق يعمل بشكل صحيح عند تغيير اللغة أو الإعدادات الإقليمية

### 8. قاعدة تنظيم ملفات التوثيق والخطط
**الهدف**: تنظيم ملفات Markdown في بنية معيارية قابلة للتخصيص

**المتطلبات**:
- تحديد مجلد مركزي مخصص للخطط والمقترحات (مثل `docs/proposals/` أو `اقتراحات تعديل/`) وتوثيق اسمه ومكانه في دليل المشروع
- اعتماد سياسة تسمية واضحة داخل المجلد (مثلاً `YYYY-MM-DD_نوع-المستند_وصف.md`) لتسهيل الفرز والأرشفة
- الاحتفاظ بالوثائق الرسمية الأساسية (`Change log.md`، `rules.md`، `README.md`، `database_schema.md`) داخل مجلد فرعي مستقل في جذر المستودع (مثل `docs/core/`) مع توثيق موضعه

**أمثلة للملفات التي توضع في مجلد الخطط/المقترحات**:
- 📋 خطط التطوير والميزات الجديدة
- 📊 تقارير المراجعة والتحليل
- 💡 مقترحات التحسين والتعديل
- 📝 ملاحظات ودراسات الجدوى
- 🔍 تقارير الأخطاء المفصلة

**الفائدة**:
- تنظيم أفضل للمشروع وقابلية توسعة البنية عند الحاجة
- سهولة الوصول للخطط والمقترحات مع إبقاء الوثائق الرسمية معزولة في مكان معروف
- إمكانية التوافق مع أدوات توثيق خارجية عبر توثيق آلية المزامنة ومواقع الملفات

### 9. قاعدة تنظيف الملفات المؤقتة والتجريبية
**الهدف**: الحفاظ على المستودع نظيفاً وخالياً من الملفات المؤقتة عبر دورة مستمرة قابلة للتطبيق على مختلف التقنيات

**المتطلبات العامة**:
- تحديد مسؤولية واضحة (فرد أو فريق) لمراجعة الملفات المؤقتة في نهاية كل دورة تطوير أو عند إغلاق المهام
- الاعتماد على قوائم تحقق أو مهام آلية (CI/CD، مهام مجدولة) لاكتشاف وإزالة الملفات غير الضرورية
- توثيق أي ملفات تجريبية تُترك مؤقتاً مع تاريخ انتهاء صلاحية واضح وخطة حذف مستقبلية
- استخدام إعدادات التجاهل (`.gitignore`, `.dockerignore`, إعدادات IDE) لمنع تسرب الملفات المؤقتة إلى المستودع قدر الإمكان

**أمثلة على ملفات يجب مراقبتها وحذفها عند انتفاء الحاجة (قابلة للتخصيص حسب التقنية)**:
- سكربتات أو أوامر migration المؤقتة
- سكربتات التهيئة أو الاختبار السريع
- نسخ احتياطية قديمة أو ملفات تصحيح (`*.backup_*`, `.log`)
- ملفات إعداد تلقائي أو مخرجات Build (`dist/`, `build/`, `node_modules/`, `coverage/`)
- ملفات بايتكود أو مخازن مؤقتة (`__pycache__/`, `.pyc`, `.DS_Store`, ملفات IDE)

**الاستثناءات**:
- الترجمات/التهيئات الرسمية التي تم اعتمادها للتوثيق
- أحدث نسخة احتياطية أو ملفات دعم لازمة للرجوع السريع
- الملفات التجريبية الموثَّقة مع تاريخ انتهاء صلاحية محدد

**عملية الحذف الآمن**:
1. التحقق من نجاح العملية أو اكتمال المهمة التي يحتاج الملف لأجلها
2. التأكد من عدم وجود اعتماد حالي أو مستقبلي على الملف (مراجعة الفريق/الملكية)
3. الاحتفاظ بنسخة في نظام التحكم بالنسخ أو أرشفة منفصلة إذا لزم الأمر
4. حذف الملف محلياً ومن المستودع، وتحديث أي توثيق يعتمد عليه

**الفوائد**:
- مستودع أنظف وأسهل في التصفح بغض النظر عن التقنية
- تقليل الالتباس الناتج عن ملفات قديمة أو مكررة
- تحسين الأداء المحلي (مساحة أقل، بناء أسرع)
- تعزيز حوكمة الملفات والالتزام بسياسات الامتثال الداخلية

### 10. قاعدة التعديل على ملف rules.md
**الهدف**: حماية ملف القواعد من التعديلات غير المقصودة

**القاعدة**:
- ⚠️ **لا تقم بالتعديل على هذا الملف (`rules.md`) إلا بأمر مباشر وصريح من المستخدم**
- ✅ يُسمح بالقراءة والاطلاع فقط
- ✅ التعديلات تتم فقط عند طلب المستخدم صراحةً
- ❌ لا تضف قواعد جديدة تلقائياً
- ❌ لا تحذف أو تعدل قواعد موجودة بدون أمر مباشر

**السبب**:
- ملف `rules.md` يحتوي على القواعد الأساسية للمشروع
- التعديلات العشوائية قد تؤثر على سير العمل
- يجب أن تكون التعديلات مدروسة ومقصودة

**الاستثناءات**:
- تصحيح أخطاء إملائية واضحة (بعد إعلام المستخدم)
- إضافة أمثلة توضيحية (بعد موافقة المستخدم)
- أي تعديل معتمد يجب توثيقه في سجل مخصص (مثل قسم في `Change log.md` أو سجل مراجعة داخلي) لضمان إمكانية تتبع القرارات

### 11. قاعدة إنشاء ملفات Markdown
**الهدف**: منع إنشاء ملفات توثيق غير ضرورية أو غير مطلوبة

**القاعدة**:
- ⚠️ **لا تقم بإنشاء أي ملفات Markdown (.md) جديدة إلا بعد سؤال المستخدم والحصول على موافقته**
- ❌ لا تنشئ ملفات تقارير أو ملخصات تلقائياً
- ❌ لا تنشئ ملفات توثيق أو خطط بدون طلب صريح
- ✅ اسأل المستخدم أولاً عن الحاجة للملف وموقعه واسمه
- ✅ عند الموافقة، التزم بالمجلد المعتمد وسياسة التسمية الموحدة (مثل `docs/proposals/YYYY-MM-DD_نوع-المستند_وصف.md`) لضمان سهولة التتبع والتنظيم

**الاستثناءات** (يُسمح بإنشائها بدون سؤال):
- لا يوجد - يجب السؤال في جميع الحالات

**السبب**:
- تجنب تراكم ملفات التوثيق غير المطلوبة
- إعطاء المستخدم التحكم الكامل في هيكلة المشروع
- تقليل الفوضى في المجلدات
- احترام تفضيلات المستخدم في التوثيق

**عند الحاجة لإنشاء ملف**:
1. أخبر المستخدم بالحاجة لإنشاء ملف
2. اقترح اسم الملف وموقعه
3. اشرح محتوى الملف المقترح
4. انتظر موافقة المستخدم
5. أنشئ الملف فقط بعد الموافقة الصريحة

### 12. قاعدة تحديث توثيق قاعدة البيانات (Database Schema Documentation)
- **الهدف**: ضمان تطابق التوثيق المرجعي للبنية مع قاعدة البيانات الفعلية عبر جميع التقنيات
- **القاعدة الأساسية**: أي تغيير في بنية قاعدة البيانات أو نموذج البيانات يجب أن يُوثَّق فوراً في المصدر المعتمد (مثل `database_schema.md`، أداة ERD، أو Wiki محددة) وفق سياسة الفريق
- **متى نُحدّث التوثيق؟**
  1. إضافة أو حذف جداول، حقول، علاقات، أو فهارس
  2. تعديل القيود، الأنواع، القيم الافتراضية، أو الخصائص المحسوبة
  3. تغيير في سلوك البيانات بين البيئات أو قواعد بيانات مختلفة
  4. إضافة منطق مرتبط بالبيانات (مثل خصائص مشتقة، مشاهد Materialized)
- **ماذا نُوثّق؟**
  - اسم الكيان ووظيفته
  - تفاصيل الحقول والقيود والقيم الافتراضية
  - العلاقات والفهارس وأي خصائص محسوبة أوTriggers
  - اختلافات البيئات (إن وُجدت) وأي استثناءات معروفة
- **الصيغ المقبولة**:
  - ملف Markdown منسق (مثال: `docs/core/database_schema.md`)
  - مخططات ERD أو أدوات مرئية (مع ربطها في الوثائق)
  - أنظمة Wiki داخلية بشرط أن تكون جزءاً من عملية التوثيق الرسمية
- **عملية المراجعة**:
  - تضمين تحديث التوثيق ضمن متطلبات Pull Request أو مراجعة الكود
  - يمكن استخدام فحص آلي يتأكد من عدم وجود تغييرات في migrations دون تحديث التوثيق
  - تحديث `Change log.md` أو سجل مراجعة عند توثيق أي تغيير
- **الفوائد**:
  - مرجع موثوق يسهل فهم البنية للمطورين الجدد والحاليين
  - تقليل الأخطاء الناتجة عن تضارب التوثيق مع الواقع
  - تسهيل الصيانة والتطوير عبر معرفة العلاقات والقيود بسرعة

---

### 13. قاعدة توحيد أسماء الجداول والفهارس (Table & Index Naming Consistency)
**الهدف**: منع تضارب أسماء الجداول والفهارس بين النماذج (Models) وقاعدة البيانات الفعلية

- **الهدف**: ضمان التطابق بين أسماء الجداول والفهارس في الكود وواقع قاعدة البيانات عبر جميع التقنيات
- **المبادئ الأساسية**:
  - اعتماد إستراتيجية تسمية قياسية للجداول والأعمدة والفهارس تُوثّق في `docs/core/naming_conventions.md` (أو ملف معتمد ضمن نفس مجلد الوثائق الأساسية في الجذر)
  - قبل تعريف أي كيان جديد في ORM أو schema code، يجب التحقق من الاسم الفعلي المستخدم في قاعدة البيانات أو التأكد من عدم وجود تضارب عند إنشائه لأول مرة
  - لا تُعرّف فهارس موجودة مسبقاً في الكود إذا تم إنشاؤها عبر migrations أو أدوات إدارة البيانات؛ حافظ على الفصل بين تعريف المخطط وتنفيذ الترحيلات
  - استخدم أدوات التحقق أو التفقد الآلي المتاحة في التقنية (مثل `alembic revision --autogenerate`، `prisma db pull`، `typeorm schema:log`، `EF Core migrations`) للتأكد من الاتساق قبل الدمج
  - وثّق أي استثناءات أو اختلافات محلية في ملف فرعي مرجعي (مثل `docs/core/db_exceptions.md`) ضمن نفس مسار الوثائق الأساسية
- **إجراءات التحقق**:
  1. إجراء مراجعة دورية (أو آلية CI) لمقارنة المخطط المعلن في الكود مع قاعدة البيانات الفعلية
  2. إنشاء نسخة احتياطية قبل تطبيق التغييرات على قاعدة البيانات في بيئات التشغيل
  3. التأكد من أن جميع البيئات (dev، staging، prod) تستخدم نفس قواعد التسمية ونفس إصدار المخطط
- **الفوائد**:
  - تقليل أخطاء التشغيل الناتجة عن اختلاف الأسماء أو تضارب الفهارس
  - تسهيل الصيانة والتوسع عبر مرجعية تسمية واحدة مشتركة
  - إمكانية تطبيق نفس النهج على أطر وقواعد بيانات مختلفة دون إعادة كتابة القاعدة لكل تقنية

### 14. قاعدة توثيق واجهات API
- **الهدف**: الحفاظ على مصدر واحد موثوق لتوثيق جميع واجهات REST وعمليات التكامل المكشوفة من المنصة
- **القاعدة الأساسية**:
  - يجب تحديث ملف مرجعي موحد (`docs/core/api_endpoints.md`) مع كل إضافة أو تعديل جوهري على أي Endpoint أو Webhook
  - يتضمن التوثيق اسم المسار، الطريقة، الوصف، متطلبات المصادقة، وأي ملاحظات أمنية أو عمليات خاصة (مثل العربون أو الصيانة)
  - أي تغيير في الإصدار أو إضافة مسار جديد يتطلب تحديث التوثيق قبل دمج الكود، مع الإشارة إلى الإصدار (`v1`, `v2`, ...)
- **المسؤولية**: المطوّر الذي ينفذ التعديل مسؤول عن تحديث التوثيق ومراجعته ضمن طلب الدمج
- **التحقق**:
  1. التأكد من تطابق التوثيق مع تعريفات OpenAPI أو Swagger المولدة آلياً
  2. تنفيذ مراجعة دورية ضمن CI للتأكد من عدم وجود اختلافات مع الملف المرجعي
  3. الإشارة إلى التحديث في ملاحظات الإصدار أو سجل التغييرات عند توفره
- **الفوائد**:
  - تقليل سوء الفهم بين فرق الواجهة الأمامية والموبايل والجهات التكاملية
  - تسهيل الامتثال الأمني وضمان أن كل Endpoint موثق بصلاحياته
  - تحسين تجربة المطورين الجدد وتقصير زمن الإعداد (Onboarding)

### 15. قاعدة مزامنة توثيق واجهات مشروع المنصة العقارية
- **الهدف**: ضمان أن يظل ملف التوثيق الخاص بالمشروع (`real_estate_platform_docs/docs/core/api_endpoints.md`) مطابقاً للمرجع العام (`docs/core/api_endpoints.md`) وأن يتم الرجوع إليه أثناء تطوير المنصة.
- **القاعدة**:
  - أي تحديث على واجهات المنصة العقارية يجب أن ينعكس دائماً في الملفين معاً خلال نفس التعديل (أو ضمن نفس الطلب).
  - يمنع حذف أو إهمال أحد الملفين؛ في حال تغيير البنية التنظيمية يجب الحصول على موافقة صريحة من المالك.
  - الإشارة في وصف طلب الدمج إلى أن ملفي التوثيق قد تم مزامنتهما.
- **التحقق**:
  1. تنفيذ فحص يدوي أو آلي للتأكد من التطابق البنيوي بين الملفين (عدد الأقسام، المسارات، المتطلبات).
  2. ضمن مراجعات الكود، التأكد من عدم وجود اختلافات غير مقصودة بين الملفين.
- **الفوائد**:
  - تسهيل مشاركة الوثائق مع الفرق التنفيذية أو الشركاء دون الاعتماد على مستودع واحد فقط.
  - منع حدوث تضارب بين وثائق المشروع والأصل المرجعي.
  - دعم حوكمة المعرفة ضمن ملف المشروع المنفصل.
